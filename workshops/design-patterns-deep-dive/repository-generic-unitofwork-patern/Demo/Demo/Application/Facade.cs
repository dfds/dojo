using Demo.Domain.Entities;

namespace Demo.Application
{
    public class Facade : IFacade
    {
        //IEmailService _emailService;

        //IMessageService _messageService;

        //IStoryService _storyService;

        //public Facade(IEmailService emailService, IMessageService messageService, IStoryService storyService)
        //{
        //    _emailService = emailService;
        //    _messageService = messageService;   
        //    _storyService = storyService;
        //}
        //public int Notify(Post post)
        //{
        //    _emailService.Send(post);
        //    _messageService.Send(post);
        //    _storyService.Send(post);
        //}
    }
}
